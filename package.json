{
  "name": "replace-keys",
  "version": "1.0.2",
  "description": "It replaces keys of objects in an array of objects",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/no-need-to-be-anonymous/replace-keys.git"
  },
  "keywords": [
    "object",
    "array",
    "replace",
    "keys"
  ],
  "author": "no need to be anonymous",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/no-need-to-be-anonymous/replace-keys/issues"
  },
  "homepage": "https://github.com/no-need-to-be-anonymous/replace-keys#readme",
  "dependencies": {
    "tslib": "^2.3.1",
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@types/node": "^16.11.6"
  }
}
